---
name: "fenfa-sop"
description: "文案多平台分发工作流：以一篇成稿（口播稿/长文/短文）为母版，自动检测其类型，改写派生为其他类型——长文（公众号/知乎）、口播（视频文案）、短文（推特）。当用户提到\"分发\"、\"改写成公众号/推特/口播\"、\"一稿多发\"、\"这篇稿子发到其他平台\"、\"repurpose\"时，务必使用本 skill。"
---

# 文案分发工作流（fenfa-sop）

输入：一篇**成稿**文案 + 分发需求（长文 / 口播 / 短文，可多选）。
输出：各目标类型的分发稿。

一次创作，多处分发。母版里的观点、案例、数字原样继承，**改的是叙事结构，不是内容**。

## 目录纪律（动手前必读）

项目根目录：`自媒体运营/`。**开工前先读根目录 `README.md` 第四节「产出落盘与备份纪律」，它是目录语义的唯一权威，与本文冲突时以 README 为准。**

要点复述：

- 分发稿只写进母版所在的项目目录 `10_项目/<日期_主题>/`，**平铺，不建子目录**（不要 `08_distribution/`）。
- 分发阶段用 `35_` 起的前缀，多平台顺延：`35_公众号_主题.md`、`36_公众号mdnice_主题.md`、`37_X推文_主题.md`。
- 母版找不到对应项目目录时，**先问用户属于哪个项目**，不要新建。
- **不许**直接往 `20_文案库/` 写。那里只接收项目完结、用户逐项确认后的备份。
- 风格参考分两处：仿**用户自己**的语感 → 读 `20_文案库/{口播文案,文章,推文}/` 的近期稿；仿**别人** → 读 `40_参考库/风格参考/`。

## 第 1 步：自动检测母版类型

按以下特征判断，并把判断结果和依据告知用户：

| 类型 | 特征 |
|---|---|
| 口播 | 300-800 字；口语句式（"你看啊"、"我跟你说"）；钩子开场 + CTA 收尾；无小标题 |
| 长文 | 1500 字以上；有小标题/分节；有教程步骤或展开论证 |
| 短文 | 数百字内；单条或编号 thread；每段可独立成立 |

特征冲突或介于两类之间时（如 1000 字无小标题的稿子），列出两种可能问用户确认，不要硬判。
母版类型与某个分发目标相同时，提示用户该目标无需派生（或只做平台微调）。

## 第 2 步：确定派生方向

每个"母版类型 → 目标类型"的组合有固定的改写方向，方向错了产出必废：

| 母版 ↓ 目标 → | 长文 | 口播 | 短文 |
|---|---|---|---|
| **口播** | **扩写**：视频讲不下的细节、步骤扩成教程；有删除清单（wenan-sop 产出）就把有价值的删减内容捞回 | — | **提炼**：抽信息点成 thread，抽金句成单帖 |
| **长文** | — | **压缩重排**：抽 2-3 个最强信息点，套口播结构，书面语转口语 | **拆解**：一文多帖，每节可拆一条 |
| **短文** | **扩写**：每条推文扩成一节，补论证和案例（素材不足时向用户要，不编造） | **重组**：thread 观点串成口播叙事线 | — |

关键：长文是**扩写**、短文是**拆解/提炼**、口播是**压缩重排**。不允许任何方向上直接复制原文交差——各平台叙事习惯不同。

母版本身就是从某类型派生来的，就不要再倒推回去（例：长文是由口播扩写来的，长文→口播没有意义）。

## 第 3 步：按目标类型生成

每个目标类型：

1. 读 1-2 篇同类型样本作风格参考（优先 `20_文案库/` 里用户自己的近期稿）。没有样本则跳过并提醒用户。
2. 按 [references/types_spec.md](references/types_spec.md) 中对应类型的结构、规格和检查清单生成，逐项过检查清单。
3. 通用规则：不虚构、不替换母版中的案例和数字；保留作者口吻；去 AI 味自查（排比堆砌、"不是A而是B"、空洞总结词），装了 `shuorenhua`/`Humanizer-zh` 时调用终检。
4. X（推特）字数按平台真实计法核对：中日韩字符按 2 计，280 上限 ≈ 140 个汉字。生成后逐条量一遍。

### 长文排版硬规则（发布前必查）

- **正文里不要用 `---` 水平分隔线。** 这是最典型的 AI 写作痕迹，读者一眼能认出来。分节靠**小标题 + 空行**，不靠横线。
- 同样适用于 `***`、`___`，以及正文与结尾 CTA、编辑备注之间的分隔。
- 唯一例外：文件顶部 YAML frontmatter 的定界符。
- 定稿前 grep 一遍 `^---$` / `^\*\*\*$` / `^___$`，命中即删，删后把多余空行压成一个。

## 输出与备份

**写文件**：分发稿写进母版所在项目目录，`35_` 起的前缀顺延，平铺不建子目录。改稿留在原文件升版本，不另起目录。

**备份**（只在项目完结时做，且必须走确认关卡）：

1. 告诉用户「本项目可以完结了」。
2. 列出备份清单：哪个文件 → 哪个目录，逐项等确认。
3. 确认后**复制**（不是移动）到 `20_文案库/` 对应功能目录：
   - 长文（公众号/知乎/X Article）→ `20_文案库/文章/`
   - 短文（X 推文/thread/金句帖）→ `20_文案库/推文/`
   - 视频口播稿（任何平台）→ `20_文案库/口播文案/`
   命名 `YYYYMMDD_主题_类型.md`，头部加 frontmatter。
   **排版变体（mdnice 粘贴版等）不单独备份**，只留在项目目录——它和成稿是同一篇。

发布节奏提醒：不同平台错峰发（视频当天，公众号 +1 天内，推特随长文）。

## 禁止事项

- 直接复制母版原文当分发稿。
- 为凑长文篇幅注水——没有可扩写的细节就写短。
- 扩写时编造母版里没有的案例、数字、个人经历。
- 未经用户确认母版类型判断就在模糊情形下直接生成。
- 长文正文里插 `---` 分隔线。
- **在项目目录里建子目录（如 `08_distribution/`），或把分发稿散到多个目录。**
- **未经用户逐项确认就往 `20_文案库/` 写文件。**
